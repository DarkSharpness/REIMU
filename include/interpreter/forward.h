#pragma once
#include <declarations.h>

// Some forward declarations to avoid circular dependencies
namespace dark {

struct Memory;
struct Device;
struct Executable;
struct RegisterFile;

} // namespace dark
